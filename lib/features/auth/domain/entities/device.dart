class DeviceEntity {
  final String? model;
  final String? version;
  final String? locale;
  final String? fcmToken;

  const DeviceEntity({this.model, this.version, this.locale, this.fcmToken});
}
